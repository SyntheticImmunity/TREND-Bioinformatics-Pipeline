# Python tools for Steps 1-3 (sample-barcode check, orientation flip, demux).
name: trend-python
channels:
  - bioconda
  - conda-forge
dependencies:
  - python=3.10
  - biopython
  - pandas
  - numpy
  - openpyxl   # required by the demultiplex script for .xlsx barcode key
